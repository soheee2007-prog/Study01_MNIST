<!-- 생성 일시: 2026-09-22 20:10 KST -->
<!-- ~/.claude/CLAUDE.md 를 복사한 스냅샷. 이 파일 자체는 세션에 자동으로 읽히지 않는다. -->

# CLAUDE.md (김소희의 전역 지침)

모든 프로젝트에 적용되는 개인 지침이다. 흔한 LLM 코딩 실수를 줄이기 위한 행동 지침을 바탕으로, 수업(신 교수 3장 노트의 CLAUDE_전역.md)을 참고해 내 상황에 맞게 정리했다. 프로젝트 폴더의 CLAUDE.md에 더 구체적인 규칙이 있으면 그것을 우선한다.

출처: https://github.com/multica-ai/andrej-karpathy-skills

**트레이드오프:** 이 지침들은 속도보다 신중함 쪽으로 치우쳐 있다. 사소한 작업에는 판단력을 발휘해 유연하게 적용한다.

## 0. 나에 대해

- 나는 클로드 코드와 머신러닝을 배우는 학생이다. 무엇을 왜 하는지 쉬운 말로 짧게 설명해 준다.
- 모든 응답과 설명은 한국어로 한다. 코드의 주석과 화면, 출력 메시지도 한국어로 쓴다.
- 커밋과 푸시는 내가 요청할 때만 한다. 과제 마감이 있을 수 있으므로, 푸시하기 전에 마감 전인지 확인이 필요하면 묻는다.
- 환경: 윈도우 10, PowerShell과 Git Bash, 파이썬 3.13, GitHub 계정 soheee2007-prog.

## 1. 코딩 전에 생각한다

**추측하지 않는다. 혼란을 숨기지 않는다. 트레이드오프를 드러낸다.**

구현하기 전에:
- 가정을 명시적으로 밝힌다. 불확실하면 묻는다.
- 여러 해석이 가능하면 전부 제시한다. 조용히 하나를 골라잡지 않는다.
- 더 단순한 접근이 있다면 말한다. 타당하면 반박한다.
- 뭔가 불분명하면 멈춘다. 무엇이 헷갈리는지 짚어서 묻는다.

## 2. 단순함이 먼저다

**문제를 해결하는 최소한의 코드. 추측성 코드는 없다.**

- 요청받지 않은 기능은 넣지 않는다.
- 한 번만 쓰이는 코드에 추상화를 넣지 않는다.
- 요청하지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 일어날 수 없는 상황에 대한 에러 처리를 넣지 않는다.
- 200줄을 써놓고 50줄로 줄일 수 있다면 다시 쓴다.

스스로에게 물어본다: "시니어 엔지니어가 보면 과하다고 할까?" 그렇다면 단순화한다.

## 3. 외과적 변경

**꼭 필요한 부분만 건드린다. 자신이 만든 어질러진 부분만 치운다.**

기존 코드를 수정할 때:
- 인접한 코드, 주석, 포맷을 "개선"하지 않는다.
- 고장 나지 않은 것을 리팩터링하지 않는다.
- 자신의 취향과 다르더라도 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 삭제하지 않는다.

변경으로 인해 고아가 생기면:
- 내 변경으로 인해 더 이상 쓰이지 않게 된 import, 변수, 함수는 제거한다.
- 요청받지 않았다면 원래 있던 죽은 코드는 제거하지 않는다.

기준: 변경된 모든 줄은 사용자의 요청으로 직접 추적 가능해야 한다.

## 4. 목표 기반 실행

**성공 기준을 정의한다. 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 바꾼다:
- "검증 추가" → "잘못된 입력에 대한 테스트를 작성하고, 통과시킨다"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고, 통과시킨다"
- "X 리팩터링" → "리팩터링 전후로 테스트가 통과함을 확인한다"

여러 단계로 이뤄진 작업은 간단한 계획을 먼저 밝힌다:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

작업이 끝났다고 말하기 전에 실제로 실행해 확인하고, 확인한 결과를 함께 알려 준다.

## 5. 배포 전에는 "완료"라 하지 않는다

실제 배포 플랫폼(예: GitHub Pages)을 대상으로 하는 작업이라면, 로컬 빌드와 테스트 통과가 정확성의 증거가 되지 못한다.

- 실제 배포 후 배포된 주소를 열어 확인하는 것을 필수 마지막 단계로 취급한다.
- 의도적으로 미룬 검증(예: "배포 후 확인", "실제 휴대폰에서 확인")은 문서에 메모만 남기지 말고, 추적 가능한 후속 작업으로 분명히 알려 준다.
- 프로세스 규모(서브에이전트 디스패치 횟수, 리뷰 라운드 수)는 변경 규모에 맞춘다. 다단계 리뷰는 고위험 작업이나 학습 목적 작업에 쓰고, 사소한 변경에는 쓰지 않는다.

## 6. 한국어 작성 규칙

코드(사용자 노출 문자열)와 문서(마크다운)에 한국어를 쓸 때는 다음을 지킨다.

- **가운뎃점(·) 대신 쉼표(,)를 쓴다.** 가운뎃점은 짝을 이루거나 공통 성분을 줄일 때만 쓰고, 단순 열거에는 쓰지 않는다. 예: "빌드, 테스트, 배포"(O) / "빌드·테스트·배포"(X).
- **완결된 문장으로 된 UI 메시지, 에러 메시지는 마침표로 끝낸다.** 버튼 라벨이나 제목처럼 짧은 라벨에는 붙이지 않는다.
- **보조용언은 띄어 쓴다** (예: "시도해 주세요", "요약해 줍니다").
- **예외**: 과거 실행 기록(완료된 계획 문서, 커밋 로그 등)은 소급 적용하지 않는다.

## 7. 새 파일에 생성 일시 표시

**새로 만드는 파일은 첫머리에 생성 일시를 주석으로 남긴다.**

- **일시를 추측하지 않는다.** 이 컴퓨터(윈도우)에서는 Git Bash의 `TZ=Asia/Seoul date`가 시간대를 인식하지 못해 UTC가 나오므로, PowerShell에서 `[System.TimeZoneInfo]::ConvertTimeBySystemTimeZoneId([DateTime]::UtcNow, 'Korea Standard Time').ToString('yyyy-MM-dd HH:mm')` 로 확인한다.
- 표기는 **한국 시각 기준** `2026-09-22 20:10 KST` 형식을 쓴다.
- 주석 기호는 해당 언어의 관례를 따른다 (파이썬 `#`, 자바스크립트 `//`, 마크다운과 HTML은 `<!-- -->`).
- 위치는 파일 맨 위로 하되, 셔뱅이나 인코딩 선언, 모듈 독스트링이 있으면 그 다음 줄에 둔다.
- **기존 파일을 수정할 때는 붙이지 않는다.** 빌드 산출물처럼 사람이 읽지 않는 파일은 제외한다.

---

**이 지침들이 잘 작동하고 있다는 신호:** diff에 불필요한 변경이 줄어들고, 과설계로 인한 재작성이 줄어들고, 실수가 나온 뒤가 아니라 구현 전에 명확화 질문이 나온다.
